import Hero from "../components/UI/Hero/Hero";
import hero from "../../src/assets/img/home/hero.png";

const Home = (props) => {
  return <Hero title="Welcome to PAWS" hero={hero} />;
};
export default Home;
